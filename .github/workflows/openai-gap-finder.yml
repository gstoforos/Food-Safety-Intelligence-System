name: FSIS OpenAI Gap Finder

on:
  schedule:
    # Daily at 10:00 ATHENS TIME.
    # Cron is duplicated for DST: one line for EEST (summer, UTC+3) and
    # one for EET (winter, UTC+2). An Athens hour-guard step below makes
    # the "wrong" DST cron exit cleanly so only one run fires per day.
    - cron: '0 7 * * *'   # 10:00 Athens EEST (summer UTC+3)
    - cron: '0 8 * * *'   # 10:00 Athens EET  (winter UTC+2)
  workflow_dispatch:
    inputs:
      since_days:
        description: "Days to look back"
        required: false
        default: "7"

permissions:
  contents: write

concurrency:
  group: fsis-data-writers
  cancel-in-progress: false

jobs:
  gap-finder:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Athens-time guard
        id: guard
        run: |
          HOUR=$(TZ=Europe/Athens date +%H)
          echo "athens_hour=$HOUR"
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "Manual dispatch — running regardless of hour."
          elif [ "$HOUR" = "10" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "Athens 10:xx — running."
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "Not 10:xx Athens (currently ${HOUR}:xx). Skipping — the other DST cron will handle it."
          fi

      - name: Checkout
        if: steps.guard.outputs.run == 'true'
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        if: steps.guard.outputs.run == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        if: steps.guard.outputs.run == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run OpenAI gap-finder
        if: steps.guard.outputs.run == 'true'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GAP_SINCE_DAYS: ${{ github.event.inputs.since_days || '7' }}
          SKIP_COMMIT: "false"
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GIT_USER_EMAIL: "fsis-bot@advfood.tech"
          GIT_USER_NAME: "FSIS Bot"
        run: |
          python -m pipeline.gap_finder_openai
