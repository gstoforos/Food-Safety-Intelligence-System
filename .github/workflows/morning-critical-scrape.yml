name: FSIS Morning Critical-Agency Scrape

# Morning 12-agency scrape at 08:00 ATHENS TIME (not UTC).
# Pairs with daily-scrape.yml which runs the full 66-agency pass at
# 17:00 Athens. This morning pass cuts Tier-1 alert latency from ~24h
# to ~12h for the agencies that produce the bulk of recalls, without
# doubling the full API load.
#
# Dispatched by FsisScheduler.gs at 08:00 Athens (workflow_dispatch
# only; no GitHub cron). The Athens-time guard below is a defensive
# net for clock drift / dispatcher delays.
#
# 2026-04-30 — SINCE_DAYS dropped 7 → 2 for daily fires. Daily passes
# don't benefit from a 7-day window — duplicates already in the corpus
# get dedup'd anyway, but the bigger window means more rows scanned
# per agency listing + more Gemini calls in the URL gate. Manual
# dispatches can still pass since_days=7 (or higher) for backfill.

on:
  workflow_dispatch:
    inputs:
      since_days:
        description: "Days to look back. Default 2 covers daily-fire deduplication. Pass 7+ for backfill via manual dispatch."
        required: false
        default: "2"

permissions:
  contents: write

concurrency:
  group: fsis-data-writers
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Athens-time guard (06-10:xx window for the 08:00 fire)
        id: guard
        run: |
          HOUR=$(TZ=Europe/Athens date +%H)
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          elif [ "$HOUR" -ge "06" ] && [ "$HOUR" -le "10" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "Not 08:xx Athens (now ${HOUR}:xx). Skipping."
          fi

      - name: Checkout
        if: steps.guard.outputs.run == 'true'
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        if: steps.guard.outputs.run == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        if: steps.guard.outputs.run == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run critical-12 subset scrape
        if: steps.guard.outputs.run == 'true'
        env:
          GEMINI_API_KEY:    ${{ secrets.GEMINI_API_KEY }}
          OPENAI_API_KEY:    ${{ secrets.OPENAI_API_KEY }}
          GEMINI_MODEL:      ${{ vars.GEMINI_MODEL || 'gemini-2.5-flash' }}
          AGENCIES_FILTER: "FDA,USDA FSIS,CFIA,RASFF (EU),FSA (UK),FSAI (IE),RappelConso (FR),BVL (DE),AESAN (ES),AGES (AT),FSANZ (AU),MPI (NZ),EFET (GR)"
          SINCE_DAYS:  ${{ github.event.inputs.since_days || '7' }}
          SKIP_COMMIT: "false"
          GIT_USER_EMAIL: "fsis-bot@advfood.tech"
          GIT_USER_NAME:  "AFTS Intelligence System"
        run: |
          python -m pipeline.run_all


      - name: Gemini URL Gate + Merge
        if: steps.guard.outputs.run == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GIT_USER_EMAIL: "fsis-bot@advfood.tech"
          GIT_USER_NAME: "FSIS Bot"
        run: |
          python -m pipeline.url_gate_gemini
          python -m pipeline.merge_master

      - name: Summary
        if: steps.guard.outputs.run == 'true' && always()
        run: |
          echo "Morning scrape complete — Athens $(TZ=Europe/Athens date)."
          echo "Next full pass at 17:00 Athens via daily-scrape.yml."
