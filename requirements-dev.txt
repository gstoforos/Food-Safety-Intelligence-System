# Test dependencies — install with: pip install -r requirements-dev.txt
#
# Kept separate from requirements.txt so the production pipeline never
# pulls pytest. CI workflows that only need to run the pipeline import
# requirements.txt; the tests workflow adds this on top.

pytest>=8.0.0
pytest-mock>=3.12.0      # for monkeypatching datetime in cutoff tests
freezegun>=1.4.0         # for freezing time in Thursday-cutoff tests

# Static undefined-name scan (tests/test_no_undefined_names.py). Added
# 2026-08-18 after `date` and `since` were used but never defined in
# pipeline/official_feeds/sources/uk.py — valid syntax, clean import,
# 484 green tests, and a NameError the moment the collector ran.
pyflakes>=3.2.0
